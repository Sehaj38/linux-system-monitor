# Linux System Monitor (C++)

A terminal-based Linux system monitor built in C++ that reads live system
data from the `/proc` filesystem and displays CPU usage, memory usage,
and running processes using a text-based UI.

## Features
- CPU usage calculation
- Memory usage display
- Process listing
- Terminal UI (ncurses)
